# 四大方向任务计划

## 执行顺序

1. **任务2 (依赖修复)** → 2. **任务1 (启动命令)** → 3. **任务3 (移除mxnet)** → 4. **任务4 (benchmark)**

---

## 任务1: 修复多卡启动 ✅ 已完成

## 任务2: 依赖兼容性修复 ✅ 已完成

## 任务3: 从代码中移除 mxnet 依赖 ✅ 已完成

## 任务4: 数据格式基准测试 ✅ 代码已完成，待运行 benchmark

---

## 当前状态

所有代码改动已完成。详细记录见 `计划_已完成.md`。

## 下一步: 运行 benchmark 测试

数据已打包到 `/data1/dataset_0605/benchmark/` (zarr 已由用户手动修正路径)。

**运行命令**:
```bash
# 进入工作目录
cd /root/zhaokj/CVLface_lmdb/cvlface/research/recognition/code/work_0605

# 单卡快速测试
/root/miniconda3/envs/cvlface/bin/python benchmark_dataformat.py \
    --data_root /data1/dataset_0605/try \
    --benchmark_root /data1/dataset_0605/benchmark \
    --batch_size 256 --num_workers 8

# 多卡完整测试
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7 /root/miniconda3/envs/cvlface/bin/fabric run \
    --strategy=ddp --devices=8 --precision="bf16-mixed" \
    benchmark_dataformat.py \
    --data_root /data1/dataset_0605/try \
    --benchmark_root /data1/dataset_0605/benchmark \
    --batch_size 256 --num_workers 8

# 只测部分格式
/root/miniconda3/envs/cvlface/bin/python benchmark_dataformat.py \
    --data_root /data1/dataset_0605/try \
    --benchmark_root /data1/dataset_0605/benchmark \
    --formats imagefolder,lmdb,recordio
```

**输出**: benchmark 结果保存为 JSON 到 `benchmark_root/benchmark_results.json`，同时打印对比表格。
